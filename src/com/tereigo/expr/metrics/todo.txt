
1. Add publish time to the metrics

1. Introduce an ability to set dynamic tags ("keys")
May be we need:
    metricRegistry.createMetricInstance("metricName", "key", "value")
    metricRegistry.createMetricInstance("metricName", "orderId", "123")

    It will create a new metric instance based on the existing metric name but one different tag
    One limitation of this is that the key is a string!

    Check if Prometheus supports metrics:
    - with the same name but different set of tags
    "filledQty", tags: ["session", "nameName"]
    "filledQty", tags: ["session", "nameName", "orderId"]

The other solution is just add 1 or many keys to all metric types:

2. Metric factory should return the same metric if the name is the same
   But we need to validate that all tags and keys are the same

3. Do we need SimpleMetric interface at all?


/////////////////////////////////////////////////////////////////////////
Potential improvements:
/////////////////////////////////////////////////////////////////////////
1. Collapse MutableLongMetric and LongMetric into a single class
   in that case SimpleLongMentric and DedupLongMetric will not have a common mutable parent
   it won't be possible to do smth like this:
   MutableLongMetric dedupMetric;
   factory.createDedup("client1_dedupMetric", true);


/////////////////////////////////////////////////////////////////////////
Done:
/////////////////////////////////////////////////////////////////////////

3. for DedupLongMetricProxy we need to throw if we tried using publish interface!!!
  so no need to create 3 levels
  DedupLongMetricProxy proxy = new DedupLongMetricProxy(new DedupLongMetricImpl(new LongMetricImpl(name)));


4. Do we need MetricProxy at all?

    @Override
    public DedupLongMetric getDelegate() {
        return delegate;
    }