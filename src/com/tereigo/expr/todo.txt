 TODO:
1. add "not in / not within / not between" operators
2. Support scientific input for Doubles: 1e6
3. Add tests for VariantImpl equals / hashCode
4. add git hash tag / gitVersion / gitDate functions
5. Add Expr cache
6. Optimize expressions by caching the functions so that instead of fetching a function from the map:
   if (isExprContext(objResult)) {
      // then fetch the function from that ExprContext
      Object funcObj = objResult.getAsExprContext().getFunction(expr.operator.lexeme);
      return callFunction(expr.result, expr.operator, funcObj, expr.args);
    }
    // otherwise it's a normal/native function call -> get the function from the global context
    Object funcObj = ctx.getFunction(expr.operator.lexeme);

do it once and store it for reuse

7. Can we merge contexts to avoid 2 lookups in this function in ExprContextCombined?
    public MutableVariant get(String name, MutableVariant result) {
        MutableVariant res = ctx1.get(name, result);
        return res != null ? res : ctx2 != null ? ctx2.get(name, result) : null;
    }

    if contexts don't change at runtime then we are allowed to merge them
    

8. Introduce performance test for evaluation

9. "in" should have expressions inside []!

10. can we remove optimize out Grouping after parsing?
    After all it's just a proxy to the underlying expression
 static class Grouping extends Expr {
    // Grouping is really a proxy to the underlying expression so it doesn't require Variant to store the result
    <R> R accept(Visitor<R> visitor) {
      return visitor.visitGroupingExpr(this);
    }
    final Expr expression;
  }

  the only drawback is that hierarchy printers will not produce the correct output

  11.

4. May be enforce supplying ExprContext during expression compilation to make sure that it's not changed during evaluation?
   It ensures that a client will not apply any GC-type ops to ExprContext during evaluation
   It also means that the original expression and it's Expr context are "immutable" after compilation.
   Not "totally immutable" because you can substitute an order still in Supplier but both both Compiled expr and its ExprContext are bound together.
   "evaluate()" will not have "ctx" in that case and it goes into ExprEvaluator constructor:
        ASTRoot root = ExprCompiler.compile("$ric == \"VOD.L\" and $productId == 123 or 5 != 2");
        ExprEvaluator exprEvaluator = new ExprEvaluator(root, ctx);

        exprEvaluator.evaluateBool();

   Cons:
   You need to have ExprContext to be able to compile the expression which is not always available. especially when you compile in front-end

7. Add string operators:
 startsWith
 endsWith
 equalsIgnoreCase
 charAt
 indexOf
 contains

 matches
 concatenate
 substring
 toLowerCase
 toUpperCase
 regexp

7. May be rename "defineLong/defineDouble/defineFunction" to "bindLong/bindFunction"

8. Should we expose objects? It may be easier than introducing individual functions
and instead of: orderQty, orderPrice, orderRic, orderTif, ...
we will expose: order() as an Object
and all other functions well be available via reflection

10. Type conversion functions and rounding: toNanos, nanoToSec
12. May be add function aliases: len / length / size, $tuid / $clientTuid
14. Do we need to add Int type? we can re-use Variant.longVal
15. May be we don't need Operator Tokens in Expr classes because Tokens are from parsing
    We really need just type of operation and may be the location of that token in the string to report an error if it happens during evaluation
    It would be also nice to validate the expression to check if the operand types are compatible when we complied AST and before we actually evaluate
    In that case we'll have clear transition objects:
    (Input) String -> (Scanner) -> Tokens -> (Parser) -> Expression/AST -> (Interpreter) -> Result
    And objects from the previous phase should not exist in the next one

16. Other functions to add:
  random
  rnd
  date
  day
  month
  year
  time
  timeMs
  nanoTime
  falconEngineTime
  falconRnd
  falconRandom
  falconEnv (PROD, UAT, QA)
  falconDeployment ALGO/GSX/ACM/MOMIC/...
  falconRegion
  falconNodeAlgoType
  falconNodeName
  falconReleaseVersion

9. Do we NIL ?
12.






Done:
5. Add tests for aliases
4. Check that "not" is followed by Grouping to avoid runtime errors:
   not "A" == "B". Because it's translated into not("A") == "B" whereas it should be: not("A" == "B")
   not enabled  => not(enabled)
   not $ric=="BT.L"  =>  not ($ric=="BT.L")
13. May be use one Variant.objVal which can be used for both String and ByteBuffer and for any other object that the use can expose?
10. Doublecheck that: Remove all foreach in evaluations
11. Make sure initialization of Environment is gc-free! We should not add entries every time for a new evaluation
5. Add more malformed expression tests and make better error handling (see testMalformedExpressions)
1. Enhance error reporting
   Cover Scanner errors with tests: see Scanner.errorMsg()
   Add the original expression to Scanner errors
   Add the original expression to Parser errors
11. Dump the original expression in the exceptions
6. Add . for strings
7. Add string operators:
 isEmpty
 length
 contains
1. Native functions: min/max/abs
  round
  roundUp
  roundDown
  roundToNearest
  toLong,
  toDouble,
  dtol,
  ltod,

1. may be introduce a new type: List
  static class List extends Expr
   in that case IN becomes a normal Binary operator
   But it might require Variant to hold list which is undesirable :-/
1. It would be nice to have % operator for reminders
4. Generate the concrete types if we know all expr types during compilation to avoid type checking when evaluate
   I'm not sure it's possible because we don't know the exact type of the expression evaluation especially if we retrieve a value from the environment
10. Rejected: TODO: make env settable so that we can run the same expressions with the different environments
    Let's not do that until the real need arises
1. Switch Environment to Long/Bool/String/Double Suppliers to avoid GC
   getRic -> should return BB,
   getProductId -> should return "long", not Long
2. Don't allow string concatenation because it's GC
11. We can compare Long vs Double for >, < but cannot compare them for equality with ==, !=
    It feels inconsistent
5. Add support for ByteBuffer
5. Add error tests for wrong input or wrong expr usage: "!1 + 2",  "$ric == 1", "(5+2", "4-5)", "4 == 4.0"
9. Rejected: Add aliases for "or" (||) and "and" (&&) - it's not similar to Python syntax
1. tereni: TODO: Split into Mutable and immutable parts to ensure the literals can't be changed
12. Make it GC free
3. tereni: TODO: do a proper comparison of Variants
1. Rejected: TODO: convert List in InOperator to set
5. Add tests for scanner
1. Verify that all parameters are of the same type in the list at expression compile/parser time, not evaluation
8. Introduce operator IN ["abc", "cde"]
4. Add type of the expression to avoid class casting
3. Add Environment and keyword functions: $PI, $tuid, $productId, ...
2. Add $ for keywords
1. Write grammar
1. Add support for Long
2. add OR/AND (see chapter 9)
7. Remove EQUAL token ('=') - we need == only for comparison
5. Convert environment to Supplier functions
   This is to be able to retrieve the values on the fly
   In that case I don't need to fill up Environment for every order for processing
5. Rename Environment into ExprEnvironment
10. Check if "!" is the same as "not"

Potential syntax:
 $tuid.contains("ABC")     - supported
 contains($tuid,"ABC")     - supported
 "abc".contains($tuid)     - supported

 Python:
if substring in fullstring:
if fullstring.find(substring) != -1:
if search(substring, fullstring):   - regexp
